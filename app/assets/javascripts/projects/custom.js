/*
  $scope.selected=null;
  $scope.selectSegment = function(id){
    $scope.selected=id;
  };
  $scope.nextEmptySegment = function(){
  }
  $scope.showMode=['empty','all'];
  $scope.showModeSegment = function(){
    $scope.showMode.reverse();
    $scope.filterSegments($scope.showMode[0]);
  }
  $scope.filterSegments = function(mode){
    // var emptySegments =[];
    // var nSegments=$scope.segments;
    // console.log(nSegments.length-1);
    // var i = 0;
   /* for(i;i<nSegments.length;++i){
      if(mode=='empty' && $scope.segments[i]['target']==''){
        emptySegments.push($scope.segments[i]['id']);
      }else {
        emptySegments.push(null);
      }
    }

  }; *//* / filterSegments */
